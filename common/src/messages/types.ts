export enum ClientMessages {
    Connect,
    PlayerMove,
    PlayerMessage,
}

export enum ServerMessages {
    PlayerJoined,
    OtherPlayerJoined,
    PlayerLeft,
    PlayerMoved,
    PlayerMessaged,
}