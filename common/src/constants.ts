export const WORLD_SIZE = 4000;
export const CELL_SIZE = 50;
export const CHUNK_SIZE = 1000;

export const CHUNKS = WORLD_SIZE / CHUNK_SIZE;
export const CELLS = CHUNK_SIZE / CELL_SIZE;

export const INVENTORY_ROWS = 9;
export const INVENTORY_COLS = 3;
export const INVENTORY_SLOTS = INVENTORY_COLS * INVENTORY_ROWS;

export const ITEM_DROP_DESPAWN_RATE = 30000;